# Copy this file to .env (in this functions/ directory) and fill in your
# Firebase Auth user id before deploying. This is NOT a secret by itself,
# but it restricts sendWorkoutToGarmin to only ever act for this one account.
#
# Find your UID in the Firebase console: Authentication -> Users -> the UID
# column next to your sign-in email.
ALLOWED_UID=
